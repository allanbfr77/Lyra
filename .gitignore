node_modules/
dist/
out/
release/
*.log

# Segredos e variáveis de ambiente (GitGuardian / tokens locais)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json

# Dados brutos do Supabase (gerados localmente; podem conter dados sensíveis)
tools/_raw-supabase-musicas.json
# Nota: os .sqlite da Bíblia agora vivem em controller/data/biblia/ e são
# versionados de propósito, pois precisam ir embutidos no instalador do controller.

# Artefatos do sistema de arquivos montado (FUSE) — aparecem ao apagar ficheiro ainda aberto
.fuse_hidden*

# Mensagem de commit temporária (git commit -F)
.commit-msg.tmp
