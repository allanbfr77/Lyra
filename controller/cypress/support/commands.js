/**
 * Comandos Cypress customizados para o Lyra Controller.
 *
 * Registre aqui comandos reutilizáveis com `Cypress.Commands.add()`.
 *
 * Exemplos de comandos úteis para o projeto:
 *
 *   // Navega para o painel de controle e espera carregamento
 *   Cypress.Commands.add('abrirPainel', () => {
 *     cy.visit('/controller.html');
 *     cy.get('#conn-bar').should('be.visible');
 *   });
 *
 *   // Dispara mudança de estado via API REST
 *   Cypress.Commands.add('enviarEstado', (payload) => {
 *     cy.request('PUT', '/api/apresentacao/estado', payload);
 *   });
 *
 * Documentação: https://on.cypress.io/custom-commands
 */
'use strict';
